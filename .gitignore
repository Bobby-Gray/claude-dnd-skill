# Runtime files generated by the display server — never commit these
display/app.log
display/app.pid
display/stats.json
display/text_log.json
display/.token

# SRD data files — fetched on demand by data_pull.py
data/

# Python
__pycache__/
*.py[cod]
*.pyo
.env
.venv/
venv/

# macOS
.DS_Store
